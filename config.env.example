# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=jamatkhana_db
POSTGRES_PORT=5432

# Security
SECRET_KEY=your-secret-key-here-change-in-production

# CORS (comma-separated; merged with built-in local + Heroku app defaults in main.py)
# Production: add your static UI origin, e.g. CORS_ORIGINS=https://my-app.vercel.app
# CORS_ORIGINS=
